{
  "name": "munpia_creator_suppoter",
  "version": "1.0.0",
  "description": "문피아 크리에이터 보조 확장프로그램",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild content.js --bundle --outfile=dist/content.bundle.js --format=esm --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "htm": "^3.1.1",
    "preact": "^10.26.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
